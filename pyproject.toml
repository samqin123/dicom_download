[project]
name = "dicom-download"
version = "0.1.0"
description = "Multi-site DICOM download tools"
requires-python = ">=3.10"
dependencies = [
    "playwright>=1.49.0",
    "pydicom>=2.4.0",
    "tomli>=2.0.1",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]

[tool.uv]
package = false
